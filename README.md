# hats-api

A small web service for point lookups in HATS parquet catalogs.

**Work in progress.** The request and response shapes are still moving, so there is no
documentation here yet — it would be wrong by the time you read it. `src/app.rs` is the
current interface, and the tests beside it are the current examples.

Documentation follows the file-server interface, once the shapes have settled.
